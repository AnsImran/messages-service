[project]
name = "microservice-sms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.32,<1",
    "httpx>=0.27,<1",
    "pydantic>=2.10,<3",
    "pydantic-settings>=2.7,<3",
    "python-dotenv>=1.0,<2",
    # Async SQLite for the durable, paced SMS batch queue. Pure-Python thin
    # wrapper over stdlib sqlite3 — runs all ops for a connection on one
    # dedicated thread, so a single long-lived connection is safe to share
    # across the worker task + API coroutines (no check_same_thread footgun).
    "aiosqlite>=0.20,<0.21",
    "prometheus-fastapi-instrumentator>=7.0",
    "opentelemetry-distro>=0.48b0",
    "opentelemetry-exporter-otlp>=1.27.0",
    # Explicit auto-instrumentors — uv venvs ship without pip so the usual
    # `opentelemetry-bootstrap -a install` step silently no-ops. Listing
    # them here guarantees `uv sync` installs them.
    "opentelemetry-instrumentation-fastapi>=0.48b0",
    "opentelemetry-instrumentation-httpx>=0.48b0",
    "opentelemetry-instrumentation-logging>=0.48b0",
]
